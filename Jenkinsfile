pipeline {

    agent any

    Stages('build'){
        
        steps{
                echo 'building the app'
        }
    }
    Stages('test'){
        
        steps{
                echo 'testing the app'
        }
    }
    Stages('deploy'){
        
        steps{
                echo 'deploy the app'
        }
    }
}